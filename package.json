{
  "name": "serverless-push-backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "^3.51.0"
  },
  "devDependencies": {
    "twilio-run": "^3.0.0-beta.3"
  },
  "engines": {
    "node": "10"
  }
}
